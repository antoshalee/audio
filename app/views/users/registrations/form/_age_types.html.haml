.items-block-checkbox
  %label Возраст
  - AgeType.scoped.in_groups(3, false) do |group|
    .col
      - group.each do |type|
        .item-input-checkbox
          %label{for: "user_speaker_attributes_age_type_ids_#{type.id}"}
            = check_box_tag "user[speaker_attributes][age_type_ids][]", type.id, f.object.age_types.include?(type), id: "user_speaker_attributes_age_type_ids_#{type.id}"
            %span= type.name
